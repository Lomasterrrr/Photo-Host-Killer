#include <iostream>

class generatealg
{
public:
    std::string Generate(size_t length = 5);
    std::string GenerateMode(std::string::size_type length);
    std::string GenerateMode1(std::size_t length);
};

